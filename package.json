{
  "scripts": {
    "build": "babel src -d dist --copy-files"
  },
  "devDependencies": {
    "babel-cli": "^6.0.0",
    "babel-preset-env": "^1.7.0",
    "babel-preset-minify": "^0.4.3"
  },
  "babel": {
    "presets": [
      [
        "env",
        {
          "targets": {
            "browsers": [
              "cover 99.5%"
            ]
          }
        }
      ],
      "minify"
    ]
  }
}
